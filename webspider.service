[Unit]
Description=Web Spider server
After=network.target

[Service]
Type=simple
ExecStart=/home/webspider/main
WorkingDirectory=/home/webspider/www
Restart=always

[Install]
WantedBy=multi-user.target
